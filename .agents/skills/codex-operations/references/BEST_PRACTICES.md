# Best Practices (codex-operations)

## Example Script
Use `scripts/safe_read.sh` to inspect files with line numbers before editing.

## How To Apply
- `scripts/safe_read.sh path/to/file`
- Use output when citing line references.

## Notes
- Keep reads non-mutating.
