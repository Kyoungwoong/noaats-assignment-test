# Best Practices (tdd-workflow)

## Example Script
Use `scripts/new_tdd_script.md` to define the TDD script per feature.

## How To Apply
- Add a one-line behavior per script.
- Keep RED steps small and deterministic.

## Notes
- If behavior changes, update `plan.md` first.
